import NewVerificationToken from "@/components/authcomponents/NewVerificationToken";

const page = () => {
  return (
    <div>
      <NewVerificationToken />
    </div>
  );
};

export default page;
